# Core app: tkinter ships with standard Python on Windows installs.
#
# Required for PNG export and render-check scripts:
Pillow>=10.4.0
#
# Recommended for Mermaid-aligned SVG sequence participant icons:
tksvg>=0.7.4
